<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minthive.mapper.PostMapper">

    <select id="selectHotFeed" resultType="com.minthive.entity.Post">
        SELECT p.*
        FROM post p
        WHERE p.audit_status = 1
          AND p.visibility = 0
          AND p.deleted = 0
        ORDER BY (
            SELECT COUNT(*)
            FROM like_collect lc
            WHERE lc.target_id = p.id AND lc.type = 1
        ) DESC, p.create_time DESC
    </select>

    <select id="selectRecommendFeed" resultType="com.minthive.entity.Post">
        SELECT p.*
        FROM post p
        WHERE p.audit_status = 1
          AND p.visibility = 0
          AND p.deleted = 0
        ORDER BY (
            (SELECT COUNT(*) FROM like_collect lc WHERE lc.target_id = p.id AND lc.type = 1) * 1 +
            (SELECT COUNT(*) FROM comment c WHERE c.post_id = p.id) * 2 +
            (SELECT COUNT(*) FROM like_collect lcc WHERE lcc.target_id = p.id AND lcc.type = 3) * 3
        ) DESC, p.create_time DESC
    </select>

    <select id="selectCandidatePool" resultType="com.minthive.entity.Post">
        SELECT p.*
        FROM post p
        WHERE p.audit_status = 1
          AND p.visibility = 0
          AND p.deleted = 0
        ORDER BY p.create_time DESC
        LIMIT #{limit}
    </select>

    <select id="selectByIdsOrdered" resultType="com.minthive.entity.Post">
        SELECT p.*
        FROM post p
        WHERE p.id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        ORDER BY FIELD(p.id,
        <foreach collection="ids" item="id" separator=",">
            #{id}
        </foreach>
        )
    </select>

</mapper>
